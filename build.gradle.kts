// 📄 Archivo: build.gradle.kts (en la RAIZ del proyecto)
plugins {
    // 1. Declara el plugin de Kotlin aquí, con 'apply false'
    kotlin("android") version "2.0.0" apply false
    // 2. NO declares otros plugins (Hilt, etc.) aquí.
}
