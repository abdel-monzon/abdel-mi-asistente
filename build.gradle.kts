// 📄 Archivo: build.gradle.kts (en la RAIZ del proyecto)
plugins {
    // Solo declara el plugin de Kotlin aquí, SIN la versión.
    // La versión será tomada automáticamente desde 'gradle/libs.versions.toml'
    kotlin("android") apply false
}
